[include]
 path = common-info
 path = private

[safe]
	directory = /opt/flutter

[alias]
    c = commit
    s = switch
    st = status
    rc = rebase --continue
    lol = log --oneline
    ds = diff --stat
    A = add -A
    cam = commit --amend --no-edit
    sync = !git pull && git push
    
